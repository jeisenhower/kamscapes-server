{
  "name": "kamscapes-prod-test",
  "version": "1.0.0",
  "description": "An app to test what is needed to deploy an Angular app with a node express backend.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Joel Eisenhower",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "http": "^0.0.1-security"
  }
}
